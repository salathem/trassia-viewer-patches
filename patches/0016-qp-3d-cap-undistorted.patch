diff --git a/apps/viewer/src/components/viewer/useRenderUpdates.ts b/apps/viewer/src/components/viewer/useRenderUpdates.ts
index ebd79c6..7d44fc1 100644
--- a/apps/viewer/src/components/viewer/useRenderUpdates.ts
+++ b/apps/viewer/src/components/viewer/useRenderUpdates.ts
@@ -20,6 +20,8 @@ import { getThemeClearColor } from '../../utils/viewportUtils.js';
 import { rendererOverlayTheme } from '@/lib/viewport-ui/overlay-theme-renderer';
 import { setClashColorsFromTheme } from '@/lib/clash/clash-colors';
 import type { ThemeMode } from '@/store/slices/uiSlice';
+// Trassia overlay (not upstream) — see overlay/apps/viewer/src/lib/ch/qp-corridor.ts
+import { chQpUndistorted } from '@/lib/ch/qp-corridor';
 
 export interface UseRenderUpdatesParams {
   rendererRef: MutableRefObject<Renderer | null>;
@@ -76,11 +78,17 @@ export function useRenderUpdates(params: UseRenderUpdatesParams): void {
     coordinateInfo,
     sectionPlaneRef,
     sectionRangeRef,
-    drawing2D,
+    drawing2D: drawing2DFromStore,
     show3DOverlay,
     showHiddenLines,
   } = params;
 
+  // Trassia (Paket V-QP): die 3D-Kappe nimmt IMMER die unverzerrte Zeichnung.
+  // Die Hoehenueberhoehung des Querprofil-Panels ist eine Sache der ANZEIGE im
+  // 2D-Blatt; in den Raum gehoben waere sie eine Kappe, die neben ihrem eigenen
+  // Modell schwebt. Ohne Ueberhoehung ist das dasselbe Objekt.
+  const drawing2D = chQpUndistorted(drawing2DFromStore);
+
   // Theme-aware clear color update, and the renderer's overlay theme (#5484):
   // selection, section-plane preview, every overlay line channel (annotation /
   // alignment / grid / DXF / LandXML lines and the section-cut outline, #5388)
