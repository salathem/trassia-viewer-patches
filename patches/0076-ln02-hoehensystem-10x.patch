diff --git a/apps/viewer/src/lib/geo/ifc-spatial-reference.test.ts b/apps/viewer/src/lib/geo/ifc-spatial-reference.test.ts
index 11092f3..0ba3ef7 100644
--- a/apps/viewer/src/lib/geo/ifc-spatial-reference.test.ts
+++ b/apps/viewer/src/lib/geo/ifc-spatial-reference.test.ts
@@ -38,5 +38,8 @@ describe('IFC spatial-reference adapter (#5048)', () => {
     assert.equal(reference('NAVD88'), 'EPSG:5703');
     assert.equal(reference('Normaal Amsterdams Peil'), 'EPSG:5709');
     assert.equal(reference('MSL'), undefined, 'MSL has no unique global EPSG vertical CRS');
+    // Trassia: Swiss height systems.
+    assert.equal(reference('LN02'), 'EPSG:5728');
+    assert.equal(reference('LHN95'), 'EPSG:5729');
   });
 });
diff --git a/apps/viewer/src/lib/geo/ifc-spatial-reference.ts b/apps/viewer/src/lib/geo/ifc-spatial-reference.ts
index 9976bec..c8162cc 100644
--- a/apps/viewer/src/lib/geo/ifc-spatial-reference.ts
+++ b/apps/viewer/src/lib/geo/ifc-spatial-reference.ts
@@ -33,6 +33,12 @@ function declaredVerticalEpsg(name: string | undefined): string | undefined {
   const normalized = name?.trim().toUpperCase();
   if (normalized === 'NAVD88' || normalized === 'NORTH AMERICAN VERTICAL DATUM 1988') return 'EPSG:5703';
   if (normalized === 'NAP' || normalized === 'NORMAAL AMSTERDAMS PEIL') return 'EPSG:5709';
+  // Trassia: the Swiss height systems as Swiss IFC exports write them
+  // (IfcProjectedCRS.VerticalDatum 'LN02' / 'LHN95'). Without these two, every
+  // Swiss model beside the anchor was refused as 'vertical-crs-unknown' and
+  // stayed in its own local frame.
+  if (normalized === 'LN02' || normalized === 'LN 02') return 'EPSG:5728';
+  if (normalized === 'LHN95' || normalized === 'LHN 95') return 'EPSG:5729';
   return undefined;
 }
 
